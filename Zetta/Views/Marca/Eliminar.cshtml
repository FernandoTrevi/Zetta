@model Zetta.Models.Marca
@{
    ViewData["Title"] = "Eliminar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<form method="post" asp-action="Eliminar">
    <input asp-for="Id" hidden />
    <div class="border p-3 main-container">

        <div asp-validation-summary="ModelOnly" class="text-danger">
        </div>

        <div class="form-group row">
            <h2 class="text-danger">Esta seguro de Eliminar esta Marca?</h2>
        </div>
        <hr />

        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <label asp-for="Nombre" class="form-label">Nombre de Marca</label>
                    <input asp-for="Nombre" disable class="form-control" />
                </div>
            </div>

            <br />

            <div class="row">
                <div class="col-6 col-md-6">
                    <button type="submit" class="btn btn-danger form-control">Eliminar</button>
                </div>
                <div class="col-6 col-md-6">
                    <a asp-controller="Marca" asp-action="Index" class="btn btn-secondary form-control">Regresar</a>
                </div>
            </div>
        </div>
    </div>
</form>

<style>
    .main-container {
        padding: 20px; /* Ajusta el valor según tu preferencia */
        max-width: 600px; /* Ajusta el ancho máximo según tu preferencia */
        margin: 0 auto; /* Para centrar el contenido */
    }
</style>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}

